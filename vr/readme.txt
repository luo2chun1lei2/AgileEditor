Visual Relation

目标：
  可以设定对象之间的关系，从而简化实现的细节。利用描述语言来描绘关系系统，
  用描述来取代目前编程语言的实现方法。
  
设计方案：
1, 对象是系统的最基本元素。
2, 对象一类是关系，允许设定两个对象之间的联系。并且联系要保持。
3, 对象一类是实体，允许设定实体的状态，当状态变化后，能够发送消息，支持关系。
4, 对象可以通过组合生成其他对象。存在最基本的对象，比如整数、字符串等。
  
